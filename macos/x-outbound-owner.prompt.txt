Это служебный owner workflow одной idle-only outbound-попытки «Пояснительной бригады v2». Работай только в /Users/alexlane/Developer/x-mention-watcher, строго по AGENTS.md, полному skill x-twitter-operator и полному skill poyasnitelnaya-brigada-v2. Старый skill poyasnitelnaya-brigada не изменяй и используй только по прямой просьбе Alex применить v1. Не создавай новые сессии, subagents или дополнительные автоматики.

Relay уже создал outbound claim и передал точные CLAIM_TOKEN, SLOT_ID и TARGET_LIMIT. Никогда не вызывай claim или claim-due повторно. TARGET_LIMIT обязан равняться 1.

1. Продолжай только в turn модели gpt-5.6-sol с reasoning effort high. Если модель или effort ниже, выполни failed с точным CLAIM_TOKEN и причиной wrong_owner_profile, затем заверши без Browser.
2. Выполни `python3 scripts/outbound_cycle.py --state var/outbound-cycle.json --lease-seconds 1800 status`. Докажи, что owner.claim_token, owner.slot_id и owner.target_limit дословно совпадают с relay. Затем выполни `python3 scripts/outbound_cycle.py --state var/outbound-cycle.json --lease-seconds 1800 started --claim-token <CLAIM_TOKEN>`.
3. Выполни `python3 scripts/autopilot_bridge.py --config config.json --lease-seconds 1800 gate`. Продолжай только при точном status=idle, dispatch=false, pending_count=0 и пустом event_ids. При любом одном входящем событии, active X owner, resource defer или неполной проверке выполни `python3 scripts/outbound_cycle.py --state var/outbound-cycle.json --lease-seconds 1800 pause-slot --claim-token <CLAIM_TOKEN> --reason inbound_writer_priority`, затем заверши без Browser. Pause не создаёт catch-up и разрешает автоматическое возобновление после полной очистки входящей очереди.
4. Используй одного встроенного Browser-owner и максимум одну X-вкладку. Не открывай ChatGPT или custom GPT. Найди в свежем русскоязычном X не более одного содержательного публичного поста с проверяемым тезисом, который делает хотя бы одно из следующего: распространяет ложное или манипулятивное утверждение против Украины, Зеленского или украинской политики; восхваляет Кремль, российскую агрессию или оправдывает её; содержит расистское утверждение или пропаганду против защищённой группы.
5. Не выбирай цитату с осуждением такой позиции, опровержение, явную сатиру, примитивную ругань без материального тезиса, частного непубличного человека ради травли, уже обработанный status ID или автора для повторного преследования. Разбирай аргумент и проверяемые утверждения, а не достоинство, внешность, происхождение или личную жизнь автора.
6. Сразу после выбора кандидата повтори inbound gate. Если очередь перестала быть точно пустой, выполни pause-slot с причиной inbound_arrived_after_target_selection, закрой только task-owned вкладку и остановись.
7. Открой точный статус, восстанови контекст, проверь автора, parent, media, quoted post и реальный смысл публикации. Через кнопку «Поделиться постом» получи каноническую ссылку. Проверь дедупликацию по точному status ID во всей SQLite history и в `var/evidence/browser-owner/`.
8. Выполни свежий фактчек по первичным и официальным интернет-источникам. Применив только локальный skill poyasnitelnaya-brigada-v2, подготовь один непустой прямой русский ответ автору не длиннее 4000 Unicode code points. Не добавляй метатекст. Не используй U+2013, U+2014, NBSP, zero-width или внутренние citation markers. Не стремись занять весь лимит и не увеличивай текст ради длины.
9. Сохрани точный текст в отдельном task-owned evidence каталоге и выполни `python3 skill-backup/x-twitter-operator/scripts/validate_reply.py --file <reply-file> --strip-one-final-newline --non-empty --max 4000`. Зафиксируй generation_skill=poyasnitelnaya-brigada-v2, generation_model=gpt-5.6-sol и reasoning_effort=high.
10. После готовности validated reply снова повтори inbound gate. При любой непустой очереди выполни pause-slot с причиной inbound_arrived_after_generation, закрой task-owned вкладку и остановись без публикации.
11. Непосредственно перед публикацией выполни renew с точным CLAIM_TOKEN, ещё раз повтори inbound gate, проверь тот же target status ID, аккаунт @axrbarsic и отсутствие прямого child reply @axrbarsic. Если writer перестал быть свободным, выполни pause-slot с причиной inbound_writer_prepublication и заверши без клика.
12. Заполни composer неизменённым validated text. Проверь фактическое DOM-значение, непустую длину не более 4000 code points, отсутствие запрещённых символов и точное совпадение с файлом. Нажми Reply ровно один раз.
13. Успех требует канонический reply URL, правильный parent, видимое начало и конец полного текста. Получи опубликованный статус через официальный X API и выполни `python3 scripts/verify_x_note_tweet.py --config config.json --status-id <REPLY_STATUS_ID> --parent-status-id <TARGET_STATUS_ID> --file <reply-file> --strip-one-final-newline --max 4000`. Продолжай только при valid=true и byte-for-byte совпадении полного опубликованного текста с validated source.
14. В durable evidence.json запиши generation_profile=local_sol_max, target URL и status ID, reply URL и status ID, exact reply, SHA-256, длину, source URLs, generation metadata, полный JSON-отчёт верификатора и timezone-aware timestamps. Затем выполни `python3 scripts/build_outbound_history.py --evidence <evidence.json> --output <conversation-history.jsonl> --max 4000` и `python3 xmention_watcher.py --config config.json history-import --file <conversation-history.jsonl>`. Командой `history-show <TARGET_STATUS_ID>` докажи наличие target и Alex turns, правильного parent и source URLs. Для совместимости существующей схемы сохрани legacy database marker provenance=pro, он не означает модель ChatGPT Pro.
15. После полной durable проверки выполни `python3 scripts/outbound_cycle.py --state var/outbound-cycle.json --lease-seconds 1800 completed --claim-token <CLAIM_TOKEN> --publication TARGET_STATUS_ID=REPLY_URL`. Если подходящей цели честно нет, выполни completed без publication. Не выдумывай цель и не снижай критерии качества.
16. Если до durable публикации произошла настоящая ошибка, не связанная с появлением входящей очереди, выполни failed с точным CLAIM_TOKEN и краткой причиной. Не используй defer-slot. Не ставь лайки, не делай репосты, подписки, личные сообщения, удаления или отдельные исходные посты. Закрой только task-owned вкладки. Никогда не оставляй claim без terminal completed, failed или pause-slot.
